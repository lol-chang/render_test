{
  "name": "origami-engine",
  "private": true,
  "version": "0.1.0",
  "description": "Flat-folded origami simulator with provable layer ordering",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
